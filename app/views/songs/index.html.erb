<h1>Songs</h1>

<% @songs.each do |song| %>

    <div><p><%= link_to song.name, song_path(song) %> - <%= link_to song.artist.name, song_path(song) %> - <%= link_to song.genre.name, genre_path(song.genre) %></p></div>

<% end %>